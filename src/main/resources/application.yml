spring:
  application:
    name: mcp-server
  main:
    banner-mode: off
  ai:
    mcp:
      server:
        name: webflux-mcp-server
        version: 1.0.0

logging:
  level:
    org:
      springframework:
        ai:
          mcp: TRACE

service:
  store:
    url: http://localhost:8082
  order:
    url: http://localhost:8084

server:
  port: 8099
